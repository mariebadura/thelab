// All the values to fill in before (and after) launch live here.
export const siteConfig = {
  // Create a form at https://formspree.io and paste its ID (the part after /f/).
  // The form works locally with the placeholder but submissions go nowhere.
  formspreeId: "mzdnzlwv",

  // Sign up at https://www.goatcounter.com and paste your site code.
  // Leave empty to disable analytics (e.g. until the account exists).
  goatCounterCode: "",

  // Set to the real domain once one exists — used for Open Graph URLs.
  siteUrl: "https://thelab.mariebadura.com",

  contactEmail: "mail@mariebadura.com",
};
